# core 本地密钥注入模板（viperx 加载时读「配置文件同目录」的 .env → 注入 yaml 里的 ${ENV} 占位）。
#
# 用法：本目录（config/）下拷成 .env，填真实值即可 `go run main.go` / `go test`，无需手动 export。
#       优先级：真实环境变量（含 IDE Run Config）> .env。也可不建文件、直接设 shell / IDE 环境变量。
# 只需外部凭据：仅 yaml 里写 ${ENV} 的外部真实凭据要在此给值；MySQL/Redis 在 local.yaml 是明文无需设。
# 安全：.env 已 gitignore（切勿提交）；本文件 .env.example 是模板、不含真实值。
# 其他服务：chat 也用 DEEPSEEK_API_KEY / KIMI_API_KEY，各自放 chat/config/.env 同理。

# LLM providers（core article_polish / chat 服务）
DEEPSEEK_API_KEY=sk-your-deepseek-key
KIMI_API_KEY=sk-your-kimi-key

# Embedding（core 向量化 / 语义搜索）
QIANFAN_API_KEY=your-baidu-qianfan-key
