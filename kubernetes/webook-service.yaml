apiVersion: v1
kind: Service
metadata:
  name: webook-record
spec:
  selector:
    app: webook-record
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8010
  type: LoadBalancer
