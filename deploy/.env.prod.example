# ===================================================
# prod 环境变量模板（上线前改强密码）
# 发版方式：git tag webook-v1.0.0 && git push --tags
# ===================================================
GH_USER=your-github-username
IMAGE_TAG=1.0.0
FE_IMAGE_TAG=1.0.0

APP_ENV=config/prod.yaml
DEPLOY_ENV=prod

MYSQL_PASS=CHANGE_ME_STRONG
REDIS_PASS=CHANGE_ME_STRONG

# ── 业务/中间件内存（足配）
MYSQL_MEM=1536m
REDIS_MEM=256m
KAFKA_MEM=1024m
KAFKA_HEAP=-Xmx768m -Xms768m
ES_MEM=2048m
ES_HEAP=-Xms1024m -Xmx1024m
FE_MEM=512m
OLLAMA_MEM=2048m

# ── 监控栈内存
PROMETHEUS_MEM=1024m
GRAFANA_MEM=512m
ZIPKIN_MEM=1024m
ZIPKIN_HEAP=-Xms512m -Xmx512m
OTEL_MEM=512m
NGINX_MEM=256m

# ── Kafka EXTERNAL listener
KAFKA_EXTERNAL_HOST=localhost
KAFKA_EXTERNAL_PORT=9294

# ── Etcd
ETCD_ALLOW_NONE=yes
ETCD_PASS=CHANGE_ME_STRONG
ETCD_MEM=256m

# ── 宿主端口（和 dev/staging 错开，同机可切换）
NGINX_PORT=82
PROMETHEUS_PORT=9092
GRAFANA_PORT=3021
ZIPKIN_PORT=9413

# ── Grafana（prod 上线改强密码；首次启动生效，已启动改这里无效，需清 grafana-data volume）
GRAFANA_USER=admin
GRAFANA_PASS=CHANGE_ME_STRONG
GRAFANA_SMTP_ENABLED=false
GRAFANA_SMTP_HOST=smtp.qq.com:587
GRAFANA_SMTP_USER=
GRAFANA_SMTP_PASSWORD=
GRAFANA_SMTP_FROM_ADDRESS=
GRAFANA_SMTP_FROM_NAME=Webook Monitor
ALERT_EMAIL_TO=
