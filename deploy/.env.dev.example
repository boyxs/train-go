# ===================================================
# dev 环境变量模板
# ===================================================
GH_USER=your-github-username
CORE_IMAGE_TAG=main-latest
CHAT_IMAGE_TAG=main-latest
MIGRATOR_IMAGE_TAG=main-latest
COMMENT_IMAGE_TAG=main-latest
FE_IMAGE_TAG=main-latest
GHCR_REGISTRY=ghcr.io

CORE_APP_ENV=config/dev.yaml
CHAT_APP_ENV=config/dev.yaml
MIGRATOR_APP_ENV=config/dev.yaml
COMMENT_APP_ENV=config/dev.yaml
DEPLOY_ENV=dev

MYSQL_PASS=CHANGE_ME
REDIS_PASS=CHANGE_ME

# ── 业务/中间件内存
# Go 服务：mem_limit 是 cgroup 硬限，GOMEMLIMIT 是 Go 软限（应比 mem_limit 留 ~12% buffer 给堆外），GOGC=80 比默认 100 激进
CORE_MEM=384m
CORE_GOMEMLIMIT=320MiB
CORE_GOGC=80
CHAT_MEM=256m
CHAT_GOMEMLIMIT=200MiB
CHAT_GOGC=80
MIGRATOR_MEM=256m
MIGRATOR_GOMEMLIMIT=200MiB
MIGRATOR_GOGC=80
COMMENT_MEM=256m
COMMENT_GOMEMLIMIT=200MiB
COMMENT_GOGC=80
MYSQL_MEM=768m
REDIS_MEM=128m
KAFKA_MEM=768m
KAFKA_HEAP=-Xmx512m -Xms512m
ES_MEM=768m
ES_HEAP=-Xms384m -Xmx384m
FE_MEM=384m
OLLAMA_MEM=1024m

# ── 监控栈内存
PROMETHEUS_MEM=512m
GRAFANA_MEM=256m
ZIPKIN_MEM=512m
ZIPKIN_HEAP=-Xms256m -Xmx256m
OTEL_MEM=256m
NGINX_MEM=128m

# ── Kafka EXTERNAL listener（填部署机 IP；用不到可留 localhost）
KAFKA_EXTERNAL_HOST=localhost
KAFKA_EXTERNAL_PORT=9094

# ── Etcd（切到官方 quay.io/coreos/etcd 后 ALLOW_NONE / PASS 不再使用，保留供切回 bitnami 镜像时用）
# ETCD_ALLOW_NONE=yes
# ETCD_PASS=13520
ETCD_MEM=128m

# ── 宿主端口（dev 用默认即可；想避开本地占用可以改）
NGINX_PORT=80
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
# grafana root_url（告警邮件链接用）；经域名/HTTPS 访问改完整 URL，如 https://monitor.webook.com/
GRAFANA_ROOT_URL=http://localhost:3001/
ZIPKIN_PORT=9411
# CHAT_HOST_PORT=8020   # 仅 local override 用；server 模式 chat 走 docker 内网 + nginx，无需暴露

# ── Grafana
GRAFANA_USER=admin
GRAFANA_PASS=admin
GRAFANA_SMTP_ENABLED=false
GRAFANA_SMTP_HOST=smtp.qq.com:587
GRAFANA_SMTP_USER=
GRAFANA_SMTP_PASSWORD=
GRAFANA_SMTP_FROM_ADDRESS=
GRAFANA_SMTP_FROM_NAME=Webook Monitor
ALERT_EMAIL_TO=
